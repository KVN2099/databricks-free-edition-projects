resources:
  experiments:
    x-ray-classification:
      name: /Workspace/Users/${workspace.current_user.userName}/Personal/.bundle/${bundle.name}/${var.experiment}
      permissions:
        - level: CAN_MANAGE
          user_name: ${workspace.current_user.userName}
      artifact_location: dbfs:/Volumes/${var.catalog}/${resources.schemas.x_ray_schema.name}/${resources.volumes.x-ray-volume.name}/x-ray-classification-artifacts/