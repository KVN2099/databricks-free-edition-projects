resources:
  experiments:
    x-ray-classification:
      name: /Workspace/Users/${workspace.current_user.userName}/Personal/.bundle/${bundle.name}/experiment
      permissions:
        - level: CAN_MANAGE
          user_name: ${workspace.current_user.userName}
      artifact_location: dbfs:/Volumes/${var.catalog}/${var.schema}/${var.volume}/x-ray-classification-artifacts/